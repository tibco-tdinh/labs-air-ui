{
  "name": "ProjectAir-OnPrem",
  "version": "1.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve & ng build common --watch",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng build common && ng test --browsers ChromeHeadless --watch=false",
    "lint": "ng lint"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
